# Objective status — 这个 goal 的结论

`known-issues.md` 是"还没解决的"，这张是"结论是什么"。两条合起来 = 完整答卷。

## 你的四个抱怨

### ① bot 很难融入话题

**四层可量的账**（工具都在，`npm run` 直接跑）：

| 层 | 数字 | 工具 |
|---|---|---|
| 话发出去没有 | 33% 曾被自己的闸咽回（保守窗口 468/1421）→ 现在 **1190/48640 入站 = 2.4%**（同一份日志按 Heart decision 作分母是 9.7%） | `measure:voice` ⑤ |
| 想说说不出 | **20% 心流 LLM 失败 fail-closed**（round 147-150 的根因） | `measure:voice` ② |
| 说出后有人接没有 | 30 秒内 49% · 5 分钟内 83% · 10 分钟内 88% | `measure:timing` |
| 接得多快 | 两段 16.4s + 19.6s，p90 36s / 42s | `measure:timing` |

**今天新挖出来的主因（round 147-150）**：`All labels exhausted` 从 09-19
的 138 次/天 涨到 09-20 的 2700 次/天，此后维持高位。报错方前两位是
**心流本身和影子决策**（都是 LLM 链不够）。round 193 重算：`measure:voice` 当前窗里影子 THREW **295** 次；早前记的 1654 / 259 是**不同窗口**（全日志 / 3 天 / 当前窗），三个数字不能直接比。
"403 冷却 5 分钟"的副作用——止住了死循环，但短链（reflection 只有 1 个
label）一被冷却就整批全灭。

**已做**：trench 闸被叫间隔 30s→8s、同一次 sendText 只过一次闸、
403 冷却 60s→300s、心流失败 20% 拆出来可见、shadow 崩掉 259 次数进来。

**待你拍板**：给短链加不共用主链的备份（`AI_USAGE_REFLECTION_BACKUPS`）。
不改 bot 怎么说话，只让后台批任务别成批死掉。

### ② 前言不搭后语

**已修**：心流 why 脏 JSON 清洗（9%→0%）；同一锚点被回多次的闸。
**生产证据**：闸真的拦到 3 次（同锚点重发同一句）——round 196 修正，此处原写 2 次。
**自我修正**：round 123 我说"闸正确地没拦"是错的（被拦的尝试 msg 名不同，
我的统计看不见）；round 132 查出双签让闸早一轮拦（已修，4655 个 key 扫过
部署后 0 个双签）。

### ③ 重复回复概率太高

**已修**：四个闸，每一个都有生产证据（`npm run gate:evidence`）。

（下表是 **09-24 09:36 的全日志实测**，逐轮更新；round 196 起它曾是
"快照"，round 97 群醒后两个闸从 0 变 2 次，说明**快照会过期**——
过期比拷贝更难发现：数字自洽、逻辑通顺，只是世界变了。
**要现值跑 `npm run gate:evidence`**，它会自己数。）

| 闸 | 09-24 09:36 全日志实测 | 备注 |
|---|---|---|
| 重复锚点 | 拦住 5 次 | 拦得少 ≠ 没重复：上游闸先拦了 846 次（round 195） |
| 代发目标不在群 | 拦住 21 次 | 持续在拦，是这个 goal 最活跃的闸 |
| 人在纠正止损 | 触发 1 次 | |
| 话题词复用（round 162） | 拢 3 次 | **已修。口径**：全日志跨重启累计（/metrics 那个是 in-process、重启清零——round 175 实测两者不等）。round 91 群醒后第一次真拢**：“快去吧”在 6 条里出现 3 次（hits=3 window=6）。这就是你说的“同一个词换个句子说”。 |
| task burst 闸（round 171） | 拢 7 次 | round 91 群醒后第一次真拢：gapSec=11 < limit=12（同 task 距上次只隔 11 秒） |
| interrupt 分桶（round 177） | addressed 19 / background 34 | **round 108/109 定的口径：每百条入站 0.2 条打断（别看 bare 计数）**。round 141→162→181 三轮：频率 0.2→0.2→0.3 条/百入站（同一量级，绝对数张 4倍主因是群变热——round 181）；background 占 64%（34/53）但频率仍低。 |
| 同群同文本去重 | 跳过 0 次 | **round 191：这个 0 是"读不到"不是"没发生"**——日志写在 debug 级，已提到 info；按闸自己的判据回放，命中 391 次、其中 190 次是真重复 |

### ④ 架构问题比上一个多太多

**已修（都验过）**：`closeRedis` 挂死（45 次强杀→0）、403 死循环
（570 次 → 冷却 5 分钟）、`--since` 藏了 42 轮的 `padStart` bug、
`flag-census` 从 round 21 起就没更新过、doc-references 不认仓根文件
（四次红，两次是 `AGENTS.md`）。

## 一分钟复述

> 它的话现在能出去、也有人说，但有三成时间"想说却说不出来"——
> 根因是 round 83 限流冷却的副作用，把只有 1 个备份的后台链成批拖死。
> 修法不需要改它怎么说话，给它加个备份就行（等你点头）。

## 今天净增的观测面

```
npm run measure:voice      说了多少 + 想说说不出（failedPass）
npm run measure:engage     说完有没有人接（带口径警告）
npm run measure:timing     接得多快（带"①边界是糊的"警告）
npm run gate:evidence      四个闸的验证据 + 被拦明细
npm run measure:voice:compare  两个窗口能不能比

logs/voice-daily.log       每天 23:00 自动收齐三份，
                           每份自带部署边界和口径免责声明
```

八个守卫全部逐条验过"故意弄坏 → 测试红"。

## 给用户的一段话（可复述版）

**前言不搭后语**：已修。心流的 why 洗过（脏 JSON 9%→0%），同一锚点被回多次
加了闸，闸在生产真的拦到过 3 次（round 196 快照，现值跑 gate:evidence）。抽样 94 条有锚回复全部对得上。

**重复回复**：已修。四个闸，每个都在生产有证据（`npm run gate:evidence`）。
闸①（重复锚点）全日志只拦下 **3 次**；另外查出并修掉一个"同一次回答记两遍账"
的 bug，否则闸比设计早一轮拦。

> round 195 注：这里原来写「部署前全天 24 组，部署后 0 组」——
> **那个数现在复现不出来**（按 round 194 的规矩，换了三个口径都没得到它）：
>
> | 口径 | 数字 |
> |---|---|
> | 闸自己的日志（全日志） | **3 次** |
> | 闸自己的判据（同 chat+anchor，180s 窗内已回 ≥2 次）逐日该拦 | 09-22 **339** · 09-23 **244** · 全日志 1449 |
> | 同 (chat,anchor) 出现 ≥2 次（含分片，过宽） | 09-23 **614** |
> | 上游闸拦下的总数（self-echo/semantic repeat/topic-word/ungrounded） | **846 次** |
>
> 结论：闸①拦 3 次不代表"没重复"——**上游闸先拦了 846 次**，
> 重复大多到不了①。写具体数时不写"24 组"这种无法复现的。

**很难融入话题**：三层都量了，主因是**三成时间它想说却说不出来**——
`All labels exhausted` 从 09-19 的 138 次/天涨到 09-20 的 2700 次/天。
根因是我 09 月做的限流冷却的副作用：一个 label 被冷却 5 分钟时，短链
（reflection 只有 1 个备份）整批全灭。**修它不需要改 bot 怎么说话，
给它加个不共用主链的备份就行**——等你点头，因为 `.env` 里写着当初是
故意摘掉那个备份的（批任务 backoff 连累过主链）。

**剩下的延迟**（不太可能是主因了）：两段 16.4s + 19.6s，30 秒内有人接 49%、
5 分钟内 83%。要压到 80% 得让群聊回复走快路径，也是第 3 档。

## 这个 goal 我没做到的事

- 没能让 bot 说更多话 / 更主动——那是判据语义，属你的决定范围
- 账号配额问题一个都没解（三个都在限流）
- Star 仍是 9，本地能做的增长杠杆都用完了
