# 2026-09-21 · v1.0 轮记录

这一轮的目标（用户原话）：“审计 160 多个 flag 具体是什么 / 把老架构完全替换掉成新架构 /
优化新架构让后续加功能更快捷 / 版本号上正式版 v1.0 / 把 dsh 的 step 5 接入 nyatbot 的
smart group / 日记不一定只能写日记还能随笔记 / 还有什么新功能可以加 / 把 bot 回复频率降一下 /
bot 的作息和节假日 / 身份的认知加强 / 还有什么前置功能可以加比如 time gate 这种”。

下面只记**查出来的事实**和**还没做的**，不复述已提交的代码。

## 已完成（6 个 commit，全部推送 nyatos）

| commit | 内容 |
|---|---|
| `83d32e5` | 旗标审计：`scripts/flag-census.py` + `docs/flag-census.md` |
| `5a8cb4a` | `step-5-preview` 接进 smart group；修掉“新 provider 永远排不上” |
| `dfb2d1b` | 频率治理：每任务发送预算 + 被叫回复最小间隔 + 包络上限按实测重定 |
| `1528c61` | 随手日记（`free` 档从死档变成真能用）+ 版本号 0.5.0 → 1.0.0 |
| `a09b96f` | 节假日登记：`school_overrides` 空表 → 国办 2026 全量 |
| `d5d4975` | 身份认知：自我笔记改读 `reply_outcomes` |

## 查出来的事实（都有证据，不是推测）

### 旗标面

- `src/env.ts` 共 **498** 个键，其中布尔旗标 **221** 个，生产实际开着 **191** 个。
- **9 个死旗标**：`.env` 开着或默认开着，而 `src/` + `scripts/` + `packages/` 里
  `env().FLAG` / 解构 / `process.env.FLAG` 三种读法全无命中。
  `PROACTIVE_PRESSURE_ENABLED` `SCHEDULE_LLM_WAKE` `GOAL_LONG_TERM_ENABLED`
  `CORE_BLACKBOARD_ENABLED` `AGENT_PROGRESS_PING_ENABLED`（`.env=true`）、
  `REPLY_MODE_ENABLED` `REPLY_ACK_THEN_EXPAND_ENABLED`
  `TASK_PROGRESS_CODEACT_ENABLED` `TASK_PROGRESS_RESEARCH_ENABLED`（默认 true）。
  最坑的是 `GOAL_LONG_TERM_ENABLED`：注释说“可关”，而 `goals.ts` 用的是硬编码常量
  `GOAL_LONG_TERM_STALE_AFTER_SEC = 30 天`。
- **4 个假开关**：只被测试 mock、`src/` 不读。三个是 `JUDGE_PROACTIVE_*`
  （judge 的主动插话概率三件套，真机制在 heart / unified-tick，不读这三个名字），
  一个是 `CORE_BLACKBOARD_ENABLED`——`src/core/blackboard/` 有一整套实现，四个导入方
  （`agent/cognitive-workspace`、`agency-intent-adapter`、`core/promote`、
  `core/permission/gate`）全**无条件**跑，测试把它设 false 以为关掉了黑板。

  ⚠️ **2026-09-21 当晚更正**：我在这里还写过一句"CORE_BELIEF_VIEW_ENABLED /
  CORE_BLACKBOARD_ENABLED / CORE_PERMISSION_GATE_ENABLED 三个全是假开关"——
  **错了两个**。BELIEF_VIEW 在 `src/core/state.ts:49` 被读
  （`e.CORE_BELIEF_VIEW_ENABLED`），PERMISSION_GATE 在 `src/core/loop.ts:292` 被读
  （`envShim().CORE_PERMISSION_GATE_ENABLED`）。只有 BLACKBOARD 是真的没人读。
  根因：census 第一版只 grep `env().FLAG` 一种读法，而这里是 `e.FLAG` 和
  `envShim().FLAG`。bare_word 那一列其实把它们捞回来了，是我的叙述没核对就写。
  census 脚本现在显式列五种读法，并在文档里留了这条更正。
- 判定方法必须三种读法都算，否则会把活旗标误判成死的（第一版只 grep `env().FLAG`，
  差点误判 5 个）。

### 频率（用户投诉“日常过高”）

近 3 天 3,008 次群发送：小时窗 p50=6 / p90=26 / p99=63 / **max=107**；
5 分钟窗 p50=2 / p90=8 / **max=20**；最忙四群平均 14.7–19.4 条/小时。

两个病因，都是 bug 不是设计：

1. **发送预算每段归零**。executor 每段重建 `createHostApi`，`textSent` 归零，
   `maxTextSends=6` 其实是“每段 6 条 × 最多 10 段 = 60 条”。
   `semantic-dup.ts` 的注释早就写着“上限=6 —— 恰好就是 6，等于没拦”。
   证据：投递数分布恰好在 6 条处跳变（31 → 51），最差一个任务 46 秒 12 条。
2. **被叫到的回复一点间隔都没有**。最小间隔只拦主动发言，而生产流量几乎全在
   “被叫到”那条路上（2,702 次 `host sendText`：1,356 显式 `replyTo` + 1,340 只有任务默认锚点）。

另外：`TRENCH_BURST_MAX/ACTIVE` 原值 150/100 经 `envelope-backtest` 回测是**装饰性**的
（拦 0.0%）。已按实测改成 30/20（拦 11.9%，基本全是 p99 以上的失控小时）。

### 身份认知

- `runFeedbackAggregate`（每小时 cron，自我笔记的唯一写入方）只读 `feedback_events`：
  **一共 4 行**。而 `reply_outcomes` 有 **12,752 行**、带 signal，从没被自我认知读过。
- 结果：`self_model_notes` 最新一条停在 **2026-09-11**，之后十天模型没看到过关于自己的新事实。
- 近 3 天 1,208 条回复结果里 **738 条是 `ignored_5_msgs`**（“我连发 5 条没人理”），
  `user_replied` 只有 85。过度发言的代价全在这张表里。
- 已改成也聚合 `reply_outcomes`，加去重（`saveSelfNotes` 是裸 INSERT 无唯一约束，
  原实现两条文案写死，持续偏负会每小时插一条一样的）和最小样本 20。

### 节假日 / 作息（本来就有，缺的是数据）

- 作息在跑：`xxb:sleep:laststate=asleep`、9 个 `greeted` 键、sleep-cycle cron 在跳。
- 节日提示在跑：`src/shared/beijing-time.ts` 查表（公历+农历+节气，到 2027），
  经 `formatBeijingNowLine()` 进 prompt。
- **缺的是 `school_overrides` 数据**：建表以来 0 行，而 `SCHOOL_SCHEDULE_ENABLED=true`。
  不退化成“没有节假日感知”，**反向**退化——`resolveProfile()` 只对 `holiday` 返回暑假日计划，
  所以 2026-10-01～10-07 国庆里 10/1(四) 10/2(五) 10/5(一) 10/6(二) 10/7(三) 全被当成在上课；
  10/10(六) 补班日被当成自由周末。已按国办发明电〔2025〕7 号登记 33 + 6 行。

### 架构（**还没动**，下一轮的主菜）

- Core v2 **在跑**，不是死的：`CORE_V2_CHAT_IDS` 空 = **全量生效**（与 TURN_ACTOR 一致），
  log 里 757 次 `core shadow compare`。我一度以为它没跑，是看 log 才纠正的。
  但 `CORE_BELIEF_VIEW_ENABLED` / `CORE_BLACKBOARD_ENABLED` 这两个“闸”确实没人读。
- 两条 pipeline 并存：Meta+Subagent（`META_SUBAGENT_ENABLED=true`，生产主路径）和
  `processPipeline`（heart 分支）。Meta 路径**绕过** `processPipeline`，
  这就是为什么 bot 分类器/denoise 曾经在主路径上从未运行。
- `src/core/` 有 beliefs / blackboard / drives / l2 / permission / promote / agenda / skills
  一整套，其中 belief view 和 blackboard 无条件跑、旗标是摆设。

## 顺带发现但**本轮没修**的

1. **ASI rubric 列基本是常数**：`reply_outcomes` 的
   `(rubric_social_presence, warmth, competence, appropriateness, uncanny_risk, asi_final)`
   2,070 行一模一样 `(0.5, 0.5, 0.5, 0.5, 0.2, 77.0)`，560 行 `(…, 99.0)`，
   125 行 `(…, 13.0)`，只有约 20 行是真测量值。**看着有数、其实没信息**。
2. `REPLY_MODE_ENABLED` / `REPLY_ACK_THEN_EXPAND_ENABLED` 那一整个“回复形态与安全分段”
   特性既没开也没实现。
3. `xxb:trench:antiad:*` 的 scan 会捞到 `fp:` / `win:` 子键（hash/zset），
   用 `redis-cli get` 探活会误报 WRONGTYPE。

## 下一轮该做的（按依赖排序）

1. **老架构替换**：先按旗标审计把 9 个死旗标 + 4 个假开关处理掉（删 flag 或真接线），
   这是“替换”的前置——不知道什么在跑就没法删。
2. **Core v2 的两个假开关**：要么真接线（belief view / blackboard 各自由 flag 门控），
   要么删 flag。无条件跑的子系统 + 一个关不掉的开关是最难维护的组合。
3. **ASI rubric**：查清那 2,070 行常数是哪来的（是占位填充还是评分全落在默认值），
   要么修要么把那几列撤掉——假的度量比没有度量更坏。
4. **新架构扩展性**：smart-group 的 newcomer bug 已修（无数据者按池内延迟中位数归位）；
   下一个同类问题是 498 个 env 键的发现性——加一个功能要翻 1700 行 schema。
   考虑按子系统拆 `env.ts`（core / trench / meta / pipeline / tracking …）。
5. **前置功能**：用户点名 `time gate`。现有 `TIMING_*` 一族 12 个旗标，
   其中 `TIMING_GATE_PRECHECK_ENABLED` / `TIMING_GATE_HISTORY_ENABLED` 等是“LLM 之前的
   确定性层”——这一族值得单列review，因为它们正是“bot 该不该现在说话”的前置判断。

---

# 第二轮（当晚继续）

## 新增/修好的

| commit | 内容 |
|---|---|
| `3f2498a` | **视频理解**接上（step-5-preview 吃 base64 video_url）；顺带挖出 `FORMAT=claude` 分支把图片/音频/视频**全换成空字符串**的静默 bug |
| `7ecbf75` | 10 个死旗标退役；`GOAL_LONG_TERM_ENABLED` 真接上 |
| `91afd8f` | **死开关守卫**：开着的旗标必须有读者，否则 CI 红 |
| `2f8cbd3` | AGENTS.md 补「Adding a feature」一节；修掉写了三年的过期迁移号 0049→0114 |
| `65b520b` | **time gate**：61% 的"解析失败"是 `maxTokens` 写死 200 造成的 |
| `f9dfa2a` | 更正上一轮"三个 CORE_* 全是假开关"——错了两个 |

## 第二轮查出来的事实

### 视频（用户新指出）

"stepfun 模型实际上是可以视频 5min 以内输入的，vision 应该不止可以识别图片还可以识别视频"。

`multimodal.ts` 里只有一行 `[视频：用户发送了一段视频]` + "description not supported yet"。
**那个判断曾经是对的，现在过期了**——实测 step-5-preview 吃 base64 `video_url`，
6 秒测试视频准确描述了内容（彩色条纹/彩虹斜线/移动方块/左上角计时器）。

四个坑，三个是陷阱：

1. **不能蹭 vision 链**。同一个 `video_url` part 发给 step-3.7-flash 返回 **200 但
   content 为空**——token 全烧在 reasoning 上，`finish_reason=length`。
   "返回 200"不等于"看得懂"。所以加了 `AI_PROVIDER_X_VIDEO` 能力声明，
   smart-group 的 video profile 只收显式声明 true 的 label。
   方向性与 vision 相反是故意的：vision 没声明照发，video 没声明就不要。
2. **max_tokens 必须给够**（2000）。
3. **时长上限在下载之前判**（300s）。Telegram bot 下载上限 20MB（代码 10MB），
   5 分钟视频几乎必然超——现实里能描述的是短视频。
4. **绝不把 Telegram file URL 喂供应商**：那个 URL 里带着 bot token。

### 挖出的静默 bug（比功能本身重要）

全接完之后端到端跑出来是 **"我没看到你说的视频呀"**，而 prompt token 数也对得上。
原因在 `callModel`：`apiFormat === 'claude'` 时 content parts 被映射成
`p.type === 'text' ? p.text : ''` —— **图片、音频、视频全被换成空字符串**。
没有报错，token 数也对（只有文字那部分）。

修法：带媒体时绕过 claude 分支走 OpenAI 兼容裸路径（StepFun 的 `/step_plan/v1`
同时提供 `/messages` 和 `/chat/completions`，后者收 `video_url`；
Anthropic 原生 video block 在那个端点上回 400）。这条**顺带也修好了
claude-format label 的图片丢弃**。

### time gate（用户点名的"前置功能"）

TIMING_* 13 + TURN_* 17 个旗标，生产开 7 个。Meta 主路径**确实**跑 gate
（`src/meta/timing-adapter.ts` → `runTimingGate`）——我一开始猜"绕过
processPipeline 所以没跑"，看代码才纠正。

日志里它在真干活：333 no_action / 292 defer / 170 talk-value / 125 decision /
10 precheck / 620 dispatch gate suppressed。

**但 199 次 "gate parse failed"**（占 324 次的 61%）。env.ts 注释早就把这记成
"实测依据"放了很久。查下去发现 `rawSnippet` 全是空——不是解析不了，是**模型没输出**。

根因：调用点写死 `maxTokens: 200`，而 `.env` 把 `TIMING_GATE_USAGE` 设成
`reflection` → stepfun = step-3.7-flash（reasoning 模型，思维链计入 completion）。
实测同一 prompt：200 → content 空；800/2000 → 合法 JSON。
思维链吃光 200 token，JSON 没机会输出，然后 fail-closed 沉默——
**看起来像"gate 判了不说话"，实际是"gate 被截断了"**。
`labels.ts` 里 judge usage 的注释早就警告过这事，gate 是唯一还写死 200 的调用点。

已加 `TIMING_GATE_MAX_TOKENS`（默认 1200，实测合法 JSON 只需 ~260 token）。

### provider 池：33 个 label 只有 5 个成功过

`.env` 里 23 个 provider 指向 `http://127.0.0.1:3000/v1`，**那个端口上什么都没有**。
真在跑的 relay（cliproxyapi）在 **8317**、key 也不同，且它 217 个模型里**一个都没有**
这 23 个 label 要的模型名。所以不是"端口写错能改回来"，是对着一个不存在的上游。

health ledger 实测：healthy 5（stepfunthink 53922 / stepfunvision 6539 /
spark13 636 / stepfun 2 / step5 1），**zero successes 28**。
bot 照常干活是因为 auto-assign 只降权不删除——也正是因此能静默躺很久。
`initSmartGroup()` 现在启动时打一行点名。**没有**去改那 23 个 endpoint
（等于发明模型映射），也没删 label。这两个都要人决定。

## 还没做的

1. **`env.ts` 按子系统拆**。1753 行 / 494 键，加一个功能要翻整篇。改成
   "`src/env.ts` 只做组合，schema 分块"是安全的（spread 合并保持推断类型不变），
   但本轮没动手——收益是发现性，风险是碰 82% 的配置代码，值得单独一轮。
2. **`CORE_BLACKBOARD_ENABLED`**：唯一真的没人读的 CORE_* 旗标。blackboard 是
   storage 层，四个模块在用，加闸要同时管读写。留着单独一轮。
3. **ASI rubric 列是常数**（上一轮发现，仍未修）：2070 行一模一样
   `(0.5,0.5,0.5,0.5,0.2,77.0)`。
4. **23 个死 provider**：需要人决定上游是什么。

## 生产读数暂时取不到

频率治理和 time gate 修复都部署了，但重启后 50 条入站里 42 条是
`Meta path: asleep`——现在北京时间 02:00，硬作息门在睡觉，gate 根本没被调用。
修复本身由 API 实测 + 单测证明；生产读数要等白天。

已确认的间接证据：`task delivery recorded` 分布在修复前是
1:910 / 2:321 / 3:150 / 4:64 / 5:31 / **6:51** / 7:6 / 8:8 / 9:6 / 10:4 / 11:2 / **12:2**；
修复后（重启前窗口）6 条以上的任务从 79 个降到 1 个，且那一个在修复部署之前。
`>6` 的尾巴没了——发送预算在生效。

---

# 第三轮

## 做完的

| commit | 内容 |
|---|---|
| `5f145ae` | **env.ts 拆段**：1436 行 schema → 12 个 subsystem 文件；顺带修了守卫自己变瞎 |
| `a530b78` | `CORE_BLACKBOARD_ENABLED` 接上——"开着但没人读"清零 |
| `e53855a` | **ASI rubric 十个月来一次都没测到过** |

## 第三轮查出来的事实

### env.ts 拆段（最大的扩展性项）

495 个键堆在 1804 行里，加一个旗标要翻整篇。按行号机械切 12 段，
脚本先断言区间严丝合缝覆盖整个 schema 体才肯写。`src/env.ts` 372 行，只做组合。

段与键数：infra 83 / social 54 / features 51 / meta 48 / core 38 / life 38 /
timing 36 / memory 34 / turn 34 / cognition 32 / self 25 / judge 22。

⚠️ 目录叫 `env-sections/` 不叫 `env/sections/`——`src/env.ts` 是文件，
同名目录会让 `./sections/x.js` 解析到 `src/sections/`。第一版就这么错的。

**拆段当晚两个守卫差点静默失效，都是真 bug：**

1. `schema-sections.test.ts`（新）钉住：键集合 497 无丢失/无重复、
   组合后能 parse、每个段文件都被 import **且** spread（加了文件忘了接 = 整段消失）、
   `booleanFromEnv` 只有一份。
2. `no-dead-switches.test.ts` 原本**变成永真**了：
   · `parseEnvFlags()` 还在读 `src/env.ts` → 一个键都解析不到 →
     "每个开着的旗标都有读者"成了**空集合上的恒真命题**，测试全绿而 21 个死键不少
   · 读者搜索把 `src/env-sections/*` 算成读者 → **声明处冒充读者**
   两个都修了，并加 self-check（解析到 >400 个键、跨 6 个段各抽一个键），
   让"守卫自己在不在工作"可测。塞探针 flag 复验：它立刻点名。

census 也跟着改成逐段解析 + 「段索引」表 + 所有表格加「段」列。
同样的"声明处不算读者"修正补进 census——不修的话 21 个死键一夜之间全变成"有读者"。

### CORE_BLACKBOARD_ENABLED

最后一个"开着但没人读"的旗标。黑板被四个模块当存储层无条件用着。
`tests/unit/core/loop/loop.test.ts` 里甚至有个测试叫
"writes observations only when explicitly enabled"——它以为这旗标在门控什么。
它没有。**假开关比死旗标更坏，因为测试据此断言了不存在的行为。**

在 store 边界门控四个入口，关掉时写不进/读不出/状态不迁——
三个"空"都是既有正常返回，调用方本来就要处理。生产 .env 是 true，
所以修的是"关不掉"，不是"关一下"。

四个测试文件因此变色（预期）：它们 mock env() 时没带这个键 → undefined →
现在真的关。loop.test.ts 本来写着 `CORE_BLACKBOARD_ENABLED: false`，而它测的是
内层 `CORE_BLACKBOARD_OBSERVATIONS_ENABLED`——层层相扣，外层没接线时
"关着外层"是假的。改成显式打开外层并注释原因。

census：**dead_and_on 1 → 0**。

### ASI rubric：假度量

`reply_outcomes` 的 rubric 五列基本是常数：2070 行一模一样
(0.5,0.5,0.5,0.5,0.2,77.0)，只有约 20 行是真值。

两个原因叠加：
① `maxTokens: 120`。judge → stepfun = step-3.7-flash（reasoning），
   思维链计入 completion。实测 120/600 都得空 content，1200 才出正文——
   而 1200 出的是中文 markdown 评语，不是 JSON。
② 那个 label `FORMAT=claude` → `callClaude`（Anthropic /messages），
   **不吃 `response_format`**。StepFun 同一个 `/step_plan/v1` 也提供
   `/chat/completions`（OpenAI 兼容），那条路吃——实测带 json_object +
   max_tokens=1200 直接返回 `{"social_presence":0.9,...}`。

**跟视频那条 bug 同源：一个只在一条代码路径上成立的能力。**
provider.ts 的注释早就写了"AI SDK generateText 不透传 reasoning_effort/thinking，
只有 raw 路径会把这些 body 字段发出去"——没人把它联想到 jsonMode 上。

后果不止是列假：`rollAsiEmas` 把"全是 0.5"当"一直中等" Averaging 进 EMA，
`adjustHumanizerOverride` 据此调人设器。一整条自评链跑在常量上。

改法：新增 `ASI_USAGE`（默认 'asi'）+ `ASI_RUBRIC_MAX_TOKENS`（1200），
.env 配一个同厂同模型、**不设 FORMAT** 的 label 让调用走裸路径；
并加 `measured` 标记——测不到时 rubric 五列与 asi_final **全写 NULL**、不滚 EMA。
摩擦信号是确定性事实，照写。

第 2 半和修路由一样重要：只修路由的话，将来模型再抽风一次，
库里又会悄悄堆一排假的中等分。**失败的测量不该被记录成测量。**

## 还没做的

1. **23 个死 provider**（唯一需要人决定的一项）。`.env` 里 23 个 provider 指向
   `127.0.0.1:3000`（什么都没有），真 relay 在 8317 且 217 个模型里一个都没有
   它们要的名字。health ledger：33 个 label 只有 5 个成功过。
   **没有**去改 endpoint（等于发明模型映射），也没删 label。
2. `CODEACT_MAX_TURNS` / `TASK_MAX_ROUNDS` / `VERIFY_*` / `TTS_*` / `STREAMING_*` /
   `SEMANTIC_DUP_THRESHOLD` / `GROUNDING_*` / `JUDGE_PROACTIVE_*` /
   `EXPERIENCE_VERIFY_MIN_SUCCESS` / `DREAMING_USAGE` 等 20 个参数型键：
   grep 不到读取点但可能被脚本或别处按名取用，删前要逐个确认——不赌。

---

# 第四轮：旗标审计收口

## 做法

上一轮留下 20 个参数型键，写的是"删前要逐个确认——不赌"。这一轮逐个确认，
用**全仓搜索**（含 `.sh`、`tests/`、`exports/`，排除 node_modules/.git/dist/data/logs），
不是只看 src/。三类结论：

### A. 接线 4 个——旗标的用途是真的，被硬编码常量绕过了

| flag | 原来的硬编码 |
|---|---|
| `GROUNDING_PRESENT_MAX` / `GROUNDING_ASKED_MAX` | `grounding-check.ts:71` 写死 `< 0.35`，而 .env 配了两个 0.35 没人读 |
| `EXPERIENCE_VERIFY_MIN_SUCCESS` | `experience-verify.ts` 的 `minSuccess = 2` 默认，唯一调用方不传 |
| `TASK_MAX_ROUNDS` | `task-store.ts` 的 `input.maxRounds ?? 6`，唯一调用方不传 |

四个的 .env 值/默认值都与硬编码值**恰好相同**，接上不改变行为，只是让它们真的可调。
和 `GOAL_LONG_TERM_ENABLED` / `CORE_BLACKBOARD_ENABLED` 同一类——**这是本轮最重要的发现模式：
一个旗标"没人读"往往不是它没用，而是它的用途被一个同名语义的常量绕过了。**

### B. 保留 2 个——grep 不到读者但**不是死键**

`TS_WEBHOOK_URL` / `PHP_WEBHOOK_URL` 由 `scripts/cutover.sh` 用 shell 读。
census 只 grep .ts/.mts/.js 所以误报。删了会弄坏 cutover。

### C. 退役 14 个——全仓核过确实没有读者

`DREAMING_USAGE` / `CODEACT_MAX_TURNS` / `TASK_PROGRESS_START_DELAY_MS` /
`JUDGE_PROACTIVE_RATE` / `JUDGE_PROACTIVE_MIN_INTERVAL_SEC` /
`JUDGE_PROACTIVE_MIN_RECENT_MSGS` / `SEMANTIC_DUP_THRESHOLD` /
`STREAMING_MIN_INTERVAL` / `STREAMING_MIN_CHARS` / `TTS_VOICE_PROBABILITY` /
`TTS_MAX_CHARS` / `TURN_UNIFIED_DECISION_ENABLED` / `VERIFY_DEFAULT_TIMEOUT` /
`VERIFY_MAX_ATTEMPTS`

两个值得记的细节：
- `JUDGE_PROACTIVE_*` 三个参数：随机主动插话机制整个已删（`rules.test.ts` 自己的注释
  写着"全部删除"），而 `JUDGE_PROACTIVE_ENABLED` **仍在用**——它门控
  `lastBotReplyAt` / `recentHumanMsgCount` 的预计算。所以留 ENABLED、删三个参数。
- `TURN_UNIFIED_DECISION_ENABLED` 的注释说"留着防 .env 报错"——**那个理由不成立**：
  zod 对未知键是剥离不是报错。留着一个基于错误理由的墓碑键，比没有更误导。

每个都在段文件原位留了注释说明为什么删。

## 旗标审计到此收口

| 指标 | 开始 | 现在 |
|---|---|---|
| 总键数 | 498 | 483 |
| 生产开着 | 191 | 185 |
| **开着但无人读** | **9** | **0** |
| **假开关（只被测试 mock）** | **4** | **0** |
| grep 不到读者 | 32 | 2（且那 2 个有主：shell 脚本） |

累计：**退役 24 个旗标、接线 6 个**（`GOAL_LONG_TERM_ENABLED`、
`CORE_BLACKBOARD_ENABLED`、`GROUNDING_PRESENT_MAX`、`GROUNDING_ASKED_MAX`、
`EXPERIENCE_VERIFY_MIN_SUCCESS`、`TASK_MAX_ROUNDS`）、加两道守卫
（死开关守卫 + 拆段守卫）+ 一份按段可导航的 census。

## 唯一剩下的

**23 个死 provider**——需要人决定：`.env` 里 23 个 provider 指向
`127.0.0.1:3000`（什么都没有），真 relay 在 8317 且 217 个模型里一个都没有它们要的
名字。health ledger：33 个 label 只有 5 个成功过。这不是代码能修的——
要么恢复原来那个上游，要么把 23 个 label 重映射到 8317 上真实存在的模型。

---

# 第五轮：新增前置功能 + provider 池病因定位

## 新增前置功能：心流 LLM 失败时的保句闸

翻日志发现一个数字不对：`heart LLM failed, fail-closed pass` **1867 次**，
`Heart decision` 7443 次 —— **25% 的心流裁决是 LLM 调用失败**。

失败原因分解：
  1194  All labels exhausted（64%，整条 fallback 链死透）
   169  Content rejected
   162+32  timeout
    60  all accounts unavailable (cooling/disabled)
    56  ECONNRESET · 38 TLS 断 · 73 rate limited · 18 connect timeout

旧行为一律 `pass` = 消息被永久丢弃。对没人叫 bot 的群聊消息没毛病，但对**直接
叫到 bot** 的那句是另一回事：有人 @ 了本喵问一件事，因为线路故障，这句话就此
消失，对方永远等不到回复。这和"无视直接提问是另一种失败"是同一条原则，
只是失败方从模型变成了线路。

`HEART_LLM_FAIL_KEEP_ADDRESSED`（默认开）：LLM 判不了 + 被直接叫到 → `wait`
（走既有 wait-anchor 机制稍后重评），不 `pass`；没被叫到仍旧 `pass`（避免线路
故障时把整群闲聊都排成重试）。

wait 这条路两个消费方都已实现（heart.ts:313 和 meta/heart-adapter.ts:222），
所以保句是真保句，不是换个姿势丢弃。

## provider 池病因定位（比"端口写错"更具体）

"All labels exhausted" 占心流失败 64%，而它和 provider 池健康状况直接相关：
heart 走 judge usage → label `stepfun`，backups 里只有 `stepfunjudge` 健康，
其余 7 个是死 provider。**链的有效深度是 2。**

另外查到 `.env.example`（git 里的规范配置）明确写着本地 relay 是
`http://127.0.0.1:8317/v1`，而生产 `.env` 写的是 `3000` —— 这是**配置漂移**，
不是"未知上游"。但 model 名那一层仍然是决策（relay 217 个模型里约一半有对应、
一半没有，gpt-5.x / minimax / mimo / hy 全无），所以仍旧没有擅自重映射。

## provider 池：本地 relay 探不通，所以"改端口"不是解法

第五轮本来想给"23 个死 provider"找一个不需要人拍板的解法。线索是
`.env.example`（git 里的规范配置）明确写着本地 relay 是 `127.0.0.1:8317`，
而生产 `.env` 写 `3000` —— 看着像单纯的配置漂移。

**实测把这条线索否掉了。** 拿 relay 自己的 key 打它自己 catalog 里的 11 个候选模型：

| 模型 | 结果 |
|---|---|
| claude-sonnet-4.5 | HTTP 503 |
| claude-haiku-4.5 | HTTP 503 |
| grok-4-1-fast-reasoning | 45s 超时 |
| grok-4-0709 | 45s 超时 |
| grok-3 | 45s 超时 |
| kimi-k2.5 | HTTP 500 |
| glm-4.7-flash | HTTP 500 |
| glm-5 | HTTP 400 |
| qwen3.6-plus | HTTP 400 |
| deepseek-r1 | HTTP 400 |
| Gemini 3.1 Flash Lite Preview | 45s 超时 |

**11 个全挂。** 所以把 23 个 provider 改指 8317 只是把错误从
"connection refused" 换成 "503/400/500/超时"，一个都不会因此可用。
机器上**没有能用的本地 LLM relay**。

这反过来解释了心流失败的头号原因：heart 走 judge usage → `stepfun`，
backups 里只有 `stepfunjudge` 健康，其余 7 个是死 provider ——
**链的有效深度是 2**。两个 stepfun 都撞限流/超时时，"All labels exhausted"
（占心流失败 64%）就发生了。

结论：这不是配置能修的。bot 的实际可用池就是那 5 个健康的远程 provider
（stepfun / stepfunjudge / stepfunthink / stepfunvision / spark13），
要扩池只能加**远程** provider。这一步仍然要人决定加哪些。

## 复现方式

```bash
python3 scripts/flag-census.py                 # 重新生成旗标清单
npx tsx scripts/envelope-backtest.mts 3 150 100 3600   # 看原包络限有多松
npx tsx scripts/phase1-runbook.sh read         # P1 实验读数（09-25 15:08 UTC 到期）
```
